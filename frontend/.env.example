# Base URL for the backend API.
# In dev, leave as /api — vite.config.js proxies it to http://localhost:8000.
# In production, point this at your deployed backend origin, e.g. https://api.example.com
VITE_API_BASE_URL=/api
