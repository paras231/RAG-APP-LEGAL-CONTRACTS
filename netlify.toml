[build]
  base = "frontend/"
  command = "npm run build"
  publish = "dist"

# Client-side routing (react-router) — let the SPA handle every path.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
